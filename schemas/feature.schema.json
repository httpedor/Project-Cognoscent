{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "feature.schema.json",
  "title": "Feature",
  "description": "Schema for a compendium Feature entry (Rpg/Compendium/Features/Feature.cs). The compendium file should be a JSON object whose keys are feature IDs.",
  "$ref": "#/$defs/Feature",

  "$defs": {
    "Expr": {
      "description": "An expression: either a DSL string (leading '=', e.g. \"= 1 + target.constitution\"; see Rpg/Scripting/DOCS.md) or a literal (number, string, boolean, null, or an array of those).",
      "oneOf": [
        { "type": "string" },
        { "type": "number" },
        { "type": "boolean" },
        { "type": "null" },
        { "type": "array" }
      ]
    },

    "CommonFields": {
      "description": "Fields shared by all feature types.",
      "properties": {
        "name": {
          "type": ["string", "null"],
          "description": "Display name of the feature."
        },
        "icon": {
          "type": ["string", "null"],
          "description": "Icon identifier."
        },
        "description": {
          "type": ["string", "null"],
          "description": "Description displayed in the UI."
        }
      }
    },

    "GateBlock": {
      "description": "A condition (boolean) expression, or an object pairing it with the reason shown when it blocks.",
      "oneOf": [
        { "$ref": "#/$defs/Expr" },
        {
          "type": "object",
          "required": ["condition"],
          "additionalProperties": false,
          "properties": {
            "condition": {
              "$ref": "#/$defs/Expr",
              "description": "Condition (boolean) expression. False blocks the action."
            },
            "reason": {
              "$ref": "#/$defs/Expr",
              "description": "String expression. Shown to the player when the action is blocked."
            }
          }
        }
      ]
    },

    "DamageModifierBlock": {
      "description": "A number expression modifying incoming or outgoing damage, or an object pairing it with the formula explaining the change.",
      "oneOf": [
        { "$ref": "#/$defs/Expr" },
        {
          "type": "object",
          "required": ["amount"],
          "additionalProperties": false,
          "properties": {
            "amount": {
              "$ref": "#/$defs/Expr",
              "description": "Number expression. The modified damage amount."
            },
            "formula": {
              "$ref": "#/$defs/Expr",
              "description": "String expression. The formula explaining what was changed."
            }
          }
        }
      ]
    },
    "StatModifiers": {
      "type": "object",
      "description": "Modifiers applied to one or more stats by this feature.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["type", "value"],
          "properties": {
            "type": {
              "$ref": "#/$defs/Expr",
              "description": "StatModifierType expression (one of: Flat, FlatPostMods, Percent, Multiplier, Capmax, Capmin, OverrideBase, OverrideFinal). How to apply the modifier."
            },
            "value": {
              "$ref": "#/$defs/Expr",
              "description": "Number expression. Amount of the modification."
            }
          }
        }
      }
    },

    "Feature": {
      "type": "object",
      "required": ["type", "name", "icon", "description"],
      "patternProperties": {
        "^type$": {
          "type": "string",
          "enum": ["attack", "damage_over_time", "simple", "condition"],
          "description": "Feature subtype."
        }
      },
      "properties": {
        "name": {
          "type": ["string", "null"],
          "description": "Display name."
        },
        "icon": {
          "type": ["string", "null"],
          "description": "Icon identifier."
        },
        "description": {
          "type": ["string", "null"],
          "description": "Description shown in the UI."
        },
        "skills": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Expr",
            "description": "String expression."
          },
          "description": "Skill compendium IDs granted by this feature.",
          "default": []
        },
        "doesGetAttacked": {
          "$ref": "#/$defs/GateBlock",
          "description": "Override for whether the entity can be attacked. 'reason' shown on block.\nCaller is this entity, Target is the attacking entity. The variables available are:\n$0 - Boolean representing whether the attack is currently hitting or not.\n$1 - The attack's damage type."
        },
        "doesAttack": {
          "$ref": "#/$defs/GateBlock",
          "description": "Override for whether this entity can attack. 'reason' shown on block.\nCaller is this entity, Target is the attacked entity. The variables available are:\n$0 - Boolean representing whether the attack is currently hitting or not.\n$1 - The attack's damage type."
        },
        "onExecuteSkill": {
          "$ref": "#/$defs/Expr",
          "description": "Effect expression. Applied when this entity executes a skill.\nCaller is this entity, Target is the skill being executed. The variables available are:\n$0 - The name of the skill.\n$1...$n - The skill's arguments."
        },
        "doesExecuteSkill": {
          "$ref": "#/$defs/GateBlock",
          "description": "Override for whether this entity can execute skills. 'reason' shown on block. \nCaller is this entity, Target is the skill being executed. The variables available are:\n$0 - The name of the skill.\n$1...$n - The skill's arguments."
        },
        "receivingDamage": {
          "$ref": "#/$defs/DamageModifierBlock",
          "description": "Modifier applied to damage received by this entity. Caller is this entity, Target is the attacking entity. The variables available are:\n$0 - The original damage amount.\n$1 - The damage type."
        },
        "attackingDamage": {
          "$ref": "#/$defs/DamageModifierBlock",
          "description": "Modifier applied to damage dealt by this entity. Caller is this entity, Target is the attacked entity. The variables available are:\n$0 - The original damage amount.\n$1 - The damage type."
        },
        "onTick": {
          "$ref": "#/$defs/Expr",
          "description": "Effect expression. Applied to the entity each tick while this feature is active."
        },
        "onEnabled": {
          "$ref": "#/$defs/Expr",
          "description": "Effect expression. Applied to the entity when this feature is enabled."
        },
        "onDisabled": {
          "$ref": "#/$defs/Expr",
          "description": "Effect expression. Applied to the entity when this feature is disabled."
        },
        "onInjured": {
          "$ref": "#/$defs/Expr",
          "description": "Effect expression. Applied to the entity when it receives damage while this feature is active.\n$0 - The injury's severity.\n$1 - The injury's type id"
        },
        "toggleable": {
          "$ref": "#/$defs/Expr",
          "description": "Condition (boolean) expression. Whether this feature can be toggled on and off.",
          "default": false
        },
        "statModifiers": {
          "$ref": "#/$defs/StatModifiers",
          "description": "Stat modifiers applied by this feature."
        }
      },
      "allOf": [
        { "$ref": "compendium-entry.schema.json#/$defs/Entry" },
        {
          "if": {
            "patternProperties": {
              "^type$": { "enum": ["condition", "damage_over_time"] }
            }
          },
          "then": {
            "properties": {
              "defaultDuration": {
                "$ref": "#/$defs/Expr",
                "description": "Number expression representing how many seconds is the default duration of this condition, if none was specified."
              }
            }
          }
        },
        {
          "if": {
            "patternProperties": {
              "^type$": { "enum": ["damage_over_time"] }
            }
          },
          "then": {
            "properties": {
              "damage_type": {
                "type": "string",
                "description": "Name of the DamageType applied each interval."
              },
              "damage": {
                "type": "number",
                "description": "Damage amount applied per interval."
              },
              "interval": {
                "type": "integer",
                "description": "Tick interval between damage applications."
              }
            },
            "required": ["damage", "damage_type", "interval"]
          }
        }
      ]
    }
  }
}
